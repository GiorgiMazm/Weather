<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/style.css">
    <title>Document</title>
</head>

<body>
<div class="weather-for-city">Weather for {{city}}, <br> {{country}}: <br> <span class="arrow">→</span> </div>

<div class="weather-wrapper">
    {{#weatherDays}}
        <div class="weather-for-day">
            <h2 class="day"> Weather for  {{key}}</h2>
            <table>
                <tr>
                    <th>Local time</th>
                    <th>Air temperature</th>
                    <th>Humidity</th>
                </tr>
                {{#value}}
                    <tr>
                        <td>{{time}}</td>
                        <td>{{temperature}}</td>
                        <td>{{humidity}}</td>
                    </tr>
                {{/value}}
            </table>
        </div>
    {{/weatherDays}}
</div>
</body>
</html>